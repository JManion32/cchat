{
  "name": "cchat-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild gateway.js --bundle --platform=node --target=node18 --format=cjs --external:bufferutil --external:utf-8-validate --outfile=dist/gateway.cjs"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
